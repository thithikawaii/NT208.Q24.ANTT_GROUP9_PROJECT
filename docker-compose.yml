version: '3.8'

services:
  web:
    image: thithikawaii/complain-app:latest 
    container_name: building-app-web
    ports:
      - "80:80"          
    environment:
      - DB_HOST=db         
      - DB_USER=root       
      - DB_PASS=${DB_PASS}
      - DB_NAME=login_api_demo
    env_file:
      - .env
    depends_on:
      - db                 
    networks:
      - project-network

  db:
    image: mysql:latest    
    container_name: building-app-db
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASS}
      - MYSQL_DATABASE=login_api_demo                  
    volumes:
      - db-data:/var/lib/mysql 
    networks:
      - project-network

networks:
  project-network:         

volumes:
  db-data:                 
